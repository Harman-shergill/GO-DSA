package main

import (
	"fmt"
)

func main() {
	a := []int{111, 12, 32, 422, 225}
	s := len(a)
	small := a[0]
	for i := 0; i < s; i++ {
		if a[i] < small {
			small = a[i]
		}
	}
	fmt.Println("smallest number is :", small)
}
// output: 12 
